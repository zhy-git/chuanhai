function initf(t, e, i, s) {
	checkIE(),
	$(document).ready(function() {
		isPromiseNone(),
		arrowMove(),
		youhuiHide(),
		new topSlide({
			pause: 5e3
		}),
		typeChange(),
		newsChange(),
		setIframeHeight()
	}),
	$(document).ready(function() {
		new subNavPin
	}),
	$(document).ready(function() {
		new houseLoansCompute(s),
		new Slide(".huxing-type li", ".huxing-list-item", ".huxing-control", 4, 16)
	}),
	$(document).ready(function() {
		{
			var s = (new Slide(".xiangce-type li", ".xiangce-item", ".xiangce-control", 5, 18), new POP(".up-pop", t, e, i));
			new sendData({
				ctn: ".base-ctn",
				btn: ".base-btn",
				aColor: "#333333",
				telCtn: ".base-tel-ctn",
				nameCtn: ".base-name-ctn",
				pop: s,
				loupan: t,
				loupanid: e,
				catid: i
			})
		}
		// jQuery("a[rel='pics']").fancybox({
		// 	overlayShow: !1,
		// 	transitionIn: "elastic",
		// 	transitionOut: "elastic"
		// })
	}),
	$(document).ready(function() {
		new loudong({
			imgEl: "#drag-bg",
			cover: "#drag-cover",
			drag: "#drag",
			parent: ".loudong-show",
			label: ".label",
			infoList: ".info-list",
			imgCheck: !0
		})
	}),
	$(document).ready(function() {
		new Voice({
			voiceEl: "#voice",
			limitTime: 21
		})
	}),
	//这里是调整观看的时间的弹窗口  5秒钟
	$(document).ready(function() {
		new videoGET("#head_video_box", 20, ".video-shadow", ".video-cover", "video-play", "video-pause", ".wap-link-erweima")
	})
}
function houseLoansCompute(t) {
	this.average_price = parseInt(t),
	this.method = !0,
	this.money = 0,
	this.down_payment = 0,
	this.down_payment_rate = 3,
	this.syd_rate = 5,
	this.gjj_rate = 5,
	this.acreage = 0,
	this.loans = 0,
	this.loans_type = 1,
	this.repayment_total = 0,
	this.interest = 0,
	this.monthly_repayment = 0,
	this.year = 1,
	this.decreasing = 0,
	this.ecCtn = "",
	this.interest_rate = {
		sy: 4.35,
		gjj: 2.75
	},
	this.init()
}
function arrowMove() {
	var t = $(".inform-base a"),
	e = t.first(),
	i = e.attr("title"),
	s = e.first().attr("typeid");
	t.parents(".inform-list").siblings().find(".btn").attr({
		"data-type": i,
		"data-typeid": s
	}),
	$(".jiangjia a").on("click",
	function() {
		var t = $(this).attr("typeid"),
		e = $(this).attr("title");
		$(".arrow").animate({
			left: "61px"
		}),
		$(this).parents(".inform-list").siblings().find(".btn").attr({
			"data-type": e,
			"data-typeid": t
		})
	}),
	$(".tuangou a").on("click",
	function() {
		var t = $(this).attr("typeid"),
		e = $(this).attr("title");
		$(".arrow").animate({
			left: "190px"
		}),
		$(this).parents(".inform-list").siblings().find(".btn").attr({
			"data-type": e,
			"data-typeid": t
		})
	})
}
function subNavPin() {
	this.el = $(".column-nav"),
	this.valve = null,
	this.check = null,
	this.oldCheck = null,
	this.section = [],
	this.oldIndex = null,
	this.init()
}
function Slide(t, e, i, s, n) {
	this.index = 0,
	this.typeList = t,
	this.itemList = e,
	this.controlEl = i,
	this.itemLength = s,
	this.itemWidth = 0,
	this.flag = !0,
	this.left = n,
	this.init()
}
function POP(t, e, i, s) {
	this.el = '<div class="pop"><i class="pop-close"></i><i class="top"></i><h4 class="title">发地址到手机</h4><strong class="des">该楼盘价格动态、开盘动态、优惠动态<br>将会第一时间发送到您的手机上！</strong><input type="text" class="ctn name-ctn" value="请输入您的称呼"><input type="hidden" class="item"><input type="text" class="ctn tel-ctn" value="请输入您的手机号"><input type="button" class="btn" value="立即订阅&nbsp;&#62;"></div><div class="pop-shadow"></div><div class="pop-prompt"><div class="cover"><i class="prompt-icon"></i><h6 class="title">提交成功</h6><p class="des">后期的信息将会发送到您手机上</p></div></div>',
	this.flag = !0,
	this.item = t,
	this.isShow = !0,
	this.loupan = e,
	this.loupanid = i,
	this.catid = s,
	this.typeid = "",
	this.type = "",
	this.nowItem = "",
	this.isPc = "",
	this.reg = /^((13[0-9])|(14[5,7])|(15[0-3,5-9])|(17[0,3,5-8])|(18[0-9])|166|198|199|(147))\d{8}$/,
	this.init()
}
function sendData(t) {
	this.btn = t.btn,
	this.ctn = t.ctn,
	this.typeid = "",
	this.telCtn = t.telCtn,
	this.nameCtn = t.nameCtn || "",
	this.beforeColor = "",
	this.afterColor = t.aColor,
	this.showModal = t.pop,
	this.loupan = t.loupan,
	this.loupanid = t.loupanid,
	this.catid = t.catid,
	this.reg = /^((13[0-9])|(14[5,7])|(15[0-3,5-9])|(17[0,3,5-8])|(18[0-9])|166|198|199|(147))\d{8}$/,
	this.init()
}
function topSlide(t) {
	this.index = t.firstIndex || 1,
	this.showEl = $(".column-slide li"),
	this.controlEl = $(".column-slide-thumb li"),
	this.pause = t.pause || 3e3,
	this.loop = t.loop || !0,
	this.inv_id = null,
	this.flag = null,
	this.imgLength = 0,
	this.init()
}
function typeChange() {
	$(".marker-type-change li").on("click",
	function() {
		var t = $(this).index();
		$(this).addClass("active").siblings().removeClass("active"),
		$(".xihuan-ctn ul").eq(t).show().siblings().hide()
	})
}
function loudong(t) {
	this.imgEl = $(t.imgEl),
	this.cover = $(t.cover),
	this.drag = $(t.drag),
	this.parent = $(t.parent),
	this.imgCheck = t.imgCheck || !1,
	this.label = t.label,
	this.infoList = t.infoList,
	this.center = {
		left: 0,
		top: 0
	},
	this.coverW = 0,
	this.coverH = 0,
	this.typeSwitch = {
		1 : !0,
		2 : !0,
		3 : !0
	},
	this.init()
}
function Voice(t) {
	this.voiceEl = t.voiceEl,
	this.ready = !1,
	this.allTime = 0,
	this.bufferComplete = !1,
	this.progressBarWidth = 0,
	this.nowTime = 0,
	this.currentTime = 0,
	this.progressStart = 0,
	this.progressEnd = 0,
	this.volumeLength = 0,
	this.dragMove = !1,
	this.play = !1,
	this.load = !0,
	this.limitTime = t.limitTime || 15,
	this.lock = !1,
	this.shadow = '<div class="voice-shadow"></div>',
	this.init()
}
function isPromiseNone() {
	try {
		{
			new Promise(function() {})
		}
	} catch(t) {
		var e = '<script src="http://img.faakee.com/new_pc/libs/bluebird.min.js"></script>';
		$("body").append(e)
	}
}
function videoGET(t, e, i, s, n, o, a) {
	this.el = t,
	this.playel = i,
	this.conf = s,
	this.playIcon = n,
	this.pauseIcon = o,
	this.playing = !1,
	this.duration = 0,
	this.num = 0,
	this.time = e,
	this.erweima = a,
	this.init()
}
function checkIE() {
	var t = navigator.appVersion;
	t.indexOf("MSIE 8.0") >= 0 && $("#video-conf").attr("controls", "controls")
}
function youhuiHide() {
	var t = $("#youhui_cover");
	if (0 == t.length) return ! 1;
	var e = parseInt(t.css("height")),
	i = parseInt(t.children().css("height"));
	i > e && t.children().addClass("hidden")
}
function newsChange() {
	var t = $(".dongtai-nav");
	return t.length < 1 ? !1 : void $(".dongtai-nav li").on("mouseover",
	function() {
		var t = $(this).index();
		$(this).addClass("active").siblings().removeClass("active"),
		$(".column-dongtai .detail-l").eq(t).show().siblings(".detail-l").hide()
	})
}
function setIframeHeight() {
	$("#iframe-cover").on("load",
	function() {
		var t = $("#iframe-cover"),
		e = setInterval(function() {
			var i = t.contents().find("body").height() || "";
			i && (t.attr("height", i), clearInterval(e), console.log("getHeight end"))
		},
		100)
	})
}
houseLoansCompute.prototype.formula = function() {
	var t = "";
	if (3 == this.loans_type) {
		this.down_payment = this.money * this.down_payment_rate / 10,
		this.loans = this.money - this.down_payment;
		var e = this.interest_rate.sy / 12 / 100,
		i = this.interest_rate.gjj / 12 / 100,
		s = this.loans * this.syd_rate / 10,
		n = this.loans - s;
		if (this.method) this.repayment_total = 12 * this.year * s * 1e4 * e * Math.pow(1 + e, 12 * this.year) / [Math.pow(1 + e, 12 * this.year) - 1] + 12 * this.year * n * 1e4 * i * Math.pow(1 + i, 12 * this.year) / [Math.pow(1 + i, 12 * this.year) - 1],
		this.monthly_repayment = (this.repayment_total / (12 * this.year)).toFixed(2),
		this.interest = ([this.repayment_total - 1e4 * this.loans] / 1e4).toFixed(2);
		else {
			for (var o = 1e4 * s / (12 * this.year), a = 1e4 * n / (12 * this.year), r = 0, l = 0; l < 12 * this.year; l++) {
				var h = parseFloat((1e4 * s - o * l) * e) + parseFloat((1e4 * n - a * l) * i);
				t += "<li><span>第" + (l + 1) + "期</span><span>" + (parseFloat(o) + parseFloat(a)).toFixed(2) + "</span><span>" + h.toFixed(2) + "</span></li>",
				r = parseFloat(r) + h
			}
			this.interest = (r / 1e4).toFixed(2);
			var c = 1e4 * this.interest / (12 * this.year);
			this.monthly_repayment = o + a + c,
			this.decreasing = ([parseFloat(1e4 * s * e) + parseFloat(1e4 * n * i)] - [parseFloat((1e4 * s - o) * e) + parseFloat((1e4 * n - a) * i)]).toFixed(2); {
				this.interest + this.loans
			}
		}
	} else {
		var p = 1 == this.loans_type ? this.interest_rate.sy / 12 / 100 : this.interest_rate.gjj / 12 / 100;
		if (this.down_payment = this.money * this.down_payment_rate / 10, this.loans = this.money - this.down_payment, this.method) this.repayment_total = 12 * this.year * this.loans * 1e4 * p * Math.pow(1 + p, 12 * this.year) / [Math.pow(1 + p, 12 * this.year) - 1],
		this.monthly_repayment = (this.repayment_total / (12 * this.year)).toFixed(2),
		this.interest = ([this.repayment_total - 1e4 * this.loans] / 1e4).toFixed(2);
		else {
			for (var d = 1e4 * this.loans / (12 * this.year), r = 0, l = 0; l < 12 * this.year; l++) {
				var u = parseFloat((1e4 * this.loans - d * l) * p);
				r = parseFloat(r) + u,
				t += "<li><span>第" + (l + 1) + "期</span><span>" + parseFloat(d).toFixed(2) + "</span><span>" + u.toFixed(2) + "</span></li>"
			}
			this.interest = (r / 1e4).toFixed(2);
			var c = 1e4 * this.interest / (12 * this.year);
			this.monthly_repayment = d + c,
			this.decreasing = (1e4 * this.loans * p - (1e4 * this.loans - d) * p).toFixed(2); {
				this.interest + this.loans
			}
		}
	}
	this.ecCtn = t,
	this.resShow()
},
houseLoansCompute.prototype.init = function() {
	var t = this,
	e = parseInt($("#init li").first().data("code")),
	i = $("#init li").first().text();
	this.acreage = isNaN(e) ? 0 : e,
	this.money = Math.round(t.acreage * t.average_price / 1e4),
	$(".zj").val(this.money),
	$(".select").eq(0).children("em").text(i),
	$("#sy_ir").text(t.interest_rate.sy),
	$("#gjj_ir").text(t.interest_rate.gjj),
	this.listShow(),
	this.itemClick(t),
	$(".fangdai-ctn-center").on("click",
	function() {
		t.formula()
	}),
	$("body").on("click",
	function() {
		$(".options").hide()
	}),
	$(".money-ctn").on("input",
	function() {
		var t = /[^\d.]+$/g,
		e = $(this).val();
		e = e.replace(t, ""),
		e = e.replace(/^\./g, ""),
		$(this).val(e)
	}),
	$(".money-ctn").on("focus",
	function() {
		"0" == $(this).val() && $(this).val("")
	}),
	$(".zj").on("blur",
	function() {
		"" == $(this).val() ? (t.money = Math.round(t.acreage * t.average_price / 1e4), $(this).val(t.money)) : t.money = $(this).val()
	}),
	$(".fangdai-method-select a").on("click",
	function() {
		t.methodChange($(this))
	}),
	$("#bj-des").on("click",
	function() {
		t.desShow($(this))
	}),
	$(".equality-corpus-close").on("click",
	function() {
		$(".equality-corpus-ctn").hide(),
		$(".equality-corpus-shadow").fadeOut()
	})
},
houseLoansCompute.prototype.listShow = function() {
	$(".select").on("click",
	function() {
		var t = $(this),
		e = t.next(".options").css("display");
		return "block" == e ? (t.parent().siblings().find(".options").hide(), t.next(".options").hide()) : "none" == e && (t.parent().siblings().find(".options").hide(), t.next(".options").show()),
		!1
	})
},
houseLoansCompute.prototype.resShow = function() {
	if (0 != this.money) {
		var t = $("#down-payment").text().replace(/,/g, ""),
		e = $("#loans").text().replace(/,/g, ""),
		i = $("#interest").text().replace(/,/g, ""),
		s = $("#monthly-repayment").text().replace(/,/g, ""),
		n = $("#decreasing").text().replace(/,/g, "");
		if (t == this.down_payment && e == this.loans && i == this.interest && s == this.monthly_repayment) return ! 1;
		var o = new CountUp("down-payment", t, this.down_payment, 1, 2),
		a = new CountUp("loans", e, this.loans, 1),
		r = new CountUp("interest", i, this.interest, 2, 2),
		l = new CountUp("monthly-repayment", s, this.monthly_repayment, 2, 2),
		h = new CountUp("decreasing", n, this.decreasing, 2, 2);
		o.start(),
		a.start(),
		r.start(),
		l.start(),
		h.start()
	}
},
houseLoansCompute.prototype.methodChange = function(t) {
	var e = t.data("type");
	"2" == e && e ? (this.method = !1, $(".decreasing").fadeIn(100), $("#bj-des").show()) : (this.method = !0, $(".decreasing").fadeOut(100), $("#bj-des").hide()),
	t.addClass("active").siblings().removeClass("active")
},
houseLoansCompute.prototype.desShow = function() {
	var t = $(".equality-corpus-cover .list"),
	e = $(".equality-corpus-shadow"),
	i = $(".equality-corpus-ctn");
	e.fadeIn(),
	i.show(),
	t.html(this.ecCtn)
},
houseLoansCompute.prototype.itemClick = function(t) {
	$(".options li").on("click",
	function() {
		var e = $(this),
		i = e.text(),
		s = e.parent();
		switch (s.siblings(".select").children("em").text(i), s.data("type")) {
		case "acreage":
			t.acreage = parseFloat(e.data("code")),
			t.money = Math.round(t.acreage * t.average_price / 1e4),
			$(".zj").val(t.money);
			break;
		case "down_payment":
			t.down_payment_rate = parseFloat(e.data("code"));
			break;
		case "loan_type":
			t.loans_type = parseInt(e.data("code")),
			3 == t.loans_type ? $(".type").fadeIn() : $(".type").fadeOut();
			break;
		case "year":
			t.year = parseFloat(e.data("code")),
			t.year <= 5 ? (t.interest_rate.gjj = 2.75, t.interest_rate.sy = 1 == t.year ? 4.35 : 4.75) : (t.interest_rate.gjj = 3.25, t.interest_rate.sy = 4.9),
			$("#sy_ir").text(t.interest_rate.sy),
			$("#gjj_ir").text(t.interest_rate.gjj);
			break;
		case "syd":
			t.syd_rate = parseFloat(e.data("code")),
			t.gjj_rate = 10 - t.syd_rate;
			for (var n = $(".gjj li"), o = 0; o < n.length; o++) if ($(n[o]).data("code") == t.gjj_rate) {
				var a = $(n[o]).text();
				$(".gjj").siblings(".select").children("em").text(a)
			}
			break;
		case "gjj":
			t.gjj_rate = parseFloat(e.data("code")),
			t.syd_rate = 10 - t.gjj_rate;
			for (var n = $(".syd li"), o = 0; o < n.length; o++) if ($(n[o]).data("code") == t.syd_rate) {
				var a = $(n[o]).text();
				$(".syd").siblings(".select").children("em").text(a)
			}
		}
	})
},
subNavPin.prototype.init = function() {
	this.top = parseInt(this.el.offset().top),
	this.height = parseInt(this.el.height());
	var t = this;
	this.anchorJump();
	var e = this.top + this.height,
	i = $(".column-nav-pin"),
	s = $(".column-nav-base .nav-scroll"),
	n = $(".column-nav-pin .nav-scroll");
	$(window).on("scroll",
	function() {
		var o = parseInt($(this).scrollTop()),
		a = t.activeChange(o);
		return t.check = o > e ? !0 : !1,
		t.valve = t.check === t.oldCheck ? !1 : !0,
		t.valve && t.check ? (i.show(), t.oldCheck = t.check) : t.valve && !t.check && (i.hide(), t.oldCheck = t.check),
		a == t.oldIndex ? !1 : ($(s[a]).addClass("nav-active").siblings().removeClass("nav-active"), $(n[a]).addClass("nav-active").siblings().removeClass("nav-active"), void(t.oldIndex = a))
	})
},
subNavPin.prototype.anchorJump = function() {
	for (var t = $(".column-base"), e = $(".column-nav .nav-list"), i = 0; i < t.length; i++) {
		var s = $(t[i]),
		n = parseInt(s.offset().top) + parseInt(s.css("height").replace("px", "")) - 120;
		s.attr("id", "column_" + i),
		this.section.push(n)
	}
	for (var o = 0; o < e.length; o++) for (var s = $(e[o]).children(".nav-item"), a = 0; a < t.length; a++) $(s[a]).attr("data-link", "go_" + a).addClass("nav-scroll");
	$(".nav-list .nav-item").on("click",
	function() {
		var t = $(this).data("link");
		if (t) {
			var e = t.split("_").pop();
			if (0 == e) return void $("html,body").animate({
				scrollTop: "0px"
			});
			if (1 == e) return void $("html,body").animate({
				scrollTop: parseInt($("#column_" + e).offset().top - 150) + "px"
			});
			$("html,body").animate({
				scrollTop: parseInt($("#column_" + e).offset().top - 96) + "px"
			})
		}
	})
},
subNavPin.prototype.activeChange = function(t) {
	for (var e = 0,
	i = 0,
	s = this.section.length,
	n = 0; s > n; n++) {
		if (t < this.section[n] && 0 == n) {
			e = n;
			break
		}
		if (t > this.section[n] && n == this.section.length - 1) {
			e = n;
			break
		}
		if (t > this.section[n]) i = this.section[n];
		else if (i < this.section[n]) {
			e = n;
			break
		}
	}
	return e
},
Slide.prototype.init = function() {
	var t = this;
	this.setListLength(),
	$(this.typeList).on("mouseover",
	function() {
		var e = $(this).index();
		if (t.index == e) return ! 1;
		$(this).addClass("active").siblings().removeClass("active"),
		$(t.itemList).eq(e).show().siblings(this.itemList).hide();
		var i = $(t.itemList).eq(e).children("ul");
		t.checkSlideItemLength(i),
		t.index = e
	}),
	this.control()
},
Slide.prototype.setListLength = function() {
	$(this.controlEl).children(".prev").hide();
	var t = $(this.itemList).children("ul");
	if (t.length < 1) return ! 1;
	var e = t.children("li").css("width").replace("px", ""),
	i = this.left;
	this.itemWidth = parseInt(e) + parseInt(i);
	for (var s = 0; s < t.length; s++) {
		var n = $(t[s]).children("li"),
		o = n.length;
		0 == s && o > this.itemLength && $(this.controlEl).show();
		var a = parseInt(o) * parseInt(e) + parseInt(i) * parseInt(o - 1);
		$(t[s]).css({
			width: a + "px",
			position: "absolute",
			left: "0",
			"z-index": "auto",
			display: "block"
		}).attr("data-length", o)
	}
},
Slide.prototype.control = function() {
	var t = this;
	$(this.controlEl).children(".next").on("click",
	function() {
		if (t.flag) {
			t.flag = !1;
			var e = $(t.itemList).eq(t.index).children("ul"),
			i = e.css("left").replace("px", ""),
			s = parseInt(t.itemWidth) + -1 * parseInt(i),
			n = 0,
			o = parseInt(e.data("length") - t.itemLength) * parseInt(t.itemWidth);
			if (s + t.itemWidth > o && $(this).hide(), s + t.itemWidth > n) {
				var a = $(this).siblings();
				"none" == a.css("display") && a.show()
			}
			if (s > o) return t.flag = !0,
			!1;
			e.animate({
				left: -1 * s + "px"
			}),
			setTimeout(function() {
				t.flag = !0
			},
			500)
		}
	}),
	$(this.controlEl).children(".prev").on("click",
	function() {
		if (t.flag) {
			t.flag = !1;
			var e = $(t.itemList).eq(t.index).children("ul"),
			i = parseInt(e.data("length") - t.itemLength) * parseInt(t.itemWidth),
			s = 0,
			n = e.css("left").replace("px", ""),
			o = parseInt( - 1 * n - parseInt(t.itemWidth));
			if (o - t.itemWidth < s && $(this).hide(), o - t.itemWidth < i) {
				var a = $(this).siblings();
				"none" == a.css("display") && a.show()
			}
			if (s > o) return t.flag = !0,
			!1;
			e.animate({
				left: -1 * o + "px"
			}),
			setTimeout(function() {
				t.flag = !0
			},
			500)
		}
	})
},
Slide.prototype.checkSlideItemLength = function(t) {
	var e = t.data("length"),
	i = t.css("left").replace("px", "");
	i = parseInt( - 1 * i);
	var s = parseInt(e - this.itemLength) * parseInt(this.itemWidth);
	e <= this.itemLength ? $(this.controlEl).hide() : (0 >= i ? ($(this.controlEl).children(".prev").hide(), $(this.controlEl).children(".next").show()) : i >= s ? ($(this.controlEl).children(".next").hide(), $(this.controlEl).children(".prev").show()) : s > i && i > 0 && ($(this.controlEl).children(".next").show(), $(this.controlEl).children(".prev").show()), $(this.controlEl).show())
},
POP.prototype.init = function() {
	var t = this;
	$("body").append(this.el),
	$(this.item).on("click",
	function() {
		var e = t.getCtn($(this));
		t.nowItem = $(this),
		t.setCtn(e),
		t.show()
	}),
	$(".pop-close").on("click",
	function() {
		t.close()
	}),
	$(".pop-shadow").on("click",
	function() {
		t.close()
	}),
	$(".pop .ctn").on("focus",
	function() {
		var t = $(this).val();
		t == $(this)[0].defaultValue && $(this).val("")
	}),
	$(".pop .ctn").on("blur",
	function() {
		var t = $(this).val();
		"" == t && $(this).val($(this)[0].defaultValue)
	}),
	$(".pop .ctn").on("keydown",
	function() {
		var e = document.all ? window.event: arguments[0] ? arguments[0] : event,
		i = e.charCode || e.keyCode;
		if (13 == i) {
			var s = $(this).siblings(".btn");
			t.after(s)
		}
	}),
	$(".pop .btn").on("click",
	function() {
		t.after($(this))
	})
},
POP.prototype.show = function() {
	this.isShow && ($(".pop").fadeIn(), $(".pop-shadow").show())
},
POP.prototype.close = function() {
	$(".pop").hide(),
	$(".pop-shadow").fadeOut()
},
POP.prototype.showModal = function(t) {
	var e = this;
	if (!this.flag) return ! 1;
	this.flag = !1;
	var i = parseInt(t.time) || 2e3;
	"success" == t.type ? $(".prompt-icon").removeClass("icon-error icon-warning icon-loading").addClass("icon-success") : "error" == t.type ? $(".prompt-icon").removeClass("icon-success icon-warning icon-loading").addClass("icon-error") : "loading" == t.type ? ($(".prompt-icon").removeClass("icon-success icon-warning icon-error").addClass("icon-loading"), $(".pop-prompt .title").text(t.title), $(".pop-prompt .des").text(""), $(".pop-prompt").fadeIn(), this.flag = !0) : $(".prompt-icon").removeClass("icon-error icon-success icon-loading").addClass("icon-warning"),
	$(".pop-prompt .title").text(t.title),
	$(".pop-prompt .des").text(t.des),
	$(".pop-prompt").fadeIn(),
	setTimeout(function() {
		$(".pop-prompt").fadeOut(),
		e.flag = !0
	},
	i)
},
POP.prototype.getCtn = function(t) {
	var e = t.text(),
	i = t.data("des"),
	s = t.attr("title"),
	n = t.data("typeid"),
	o = t.data("pc");
	return {
		title: e,
		des: i,
		type: s,
		typeid: n,
		isPc: o
	}
},
POP.prototype.setCtn = function(t) {
	$(".pop .title").text(t.title),
	$(".pop .des").html(t.des),
	this.type = t.type,
	this.typeid = t.typeid ? t.typeid: "",
	this.isPc = t.isPc ? t.isPc: ""
},
POP.prototype.checkCtn = function(t, e, i) {
	var s = t;
	return "" == e ? (this.showModal({
		type: "error",
		title: "格式错误",
		des: "请填写您的称呼",
		time: 1e3
	}), i.siblings(".name-ctn").focus(), !1) : "" == s ? (this.showModal({
		type: "error",
		title: "号码为空",
		des: "请填写您的手机号码",
		time: 1e3
	}), i.siblings(".tel-ctn").focus(), !1) : this.reg.test(s) ? !0 : (this.showModal({
		type: "error",
		title: "格式错误",
		des: "请正确的填写您的手机号码",
		time: 1e3
	}), i.siblings(".tel-ctn").focus(), !1)
},
// 提交数据
POP.prototype.ajaxData = function(t) {
	 // console.log(t)
	var e = t.type,
	i = "&data[loupan]=" + t.loupan + "-" + e + "&data[loupanid]=" + t.loupanid + "&data[catid]=" + t.catid;
	return i += t.typeid ? "&data[typeid]=" + t.typeid: "",
	i += t.isPc ? "&data[pc]=" + t.isPc: "",
	i += t.name ? "&data[name]=" + t.name: "&data[name]=游客",
	i += "&data[tel]=" + t.phone,
	i += "&data[laiyuan]=" + window.location.href,
	// console.log(i),
	new Promise(function(t, e) {
		$.ajax({
			type: "post",
			url: "/signup.php?",
			data: i,
			success: function(i) {
				1 == i ? t() : e("1")
				console.log(i)
			},
			error: function() {
				e("2")
			}
		})
	})
},
POP.prototype.after = function(t) {
	var e = this,
	i = t.siblings(".tel-ctn"),
	s = t.siblings(".name-ctn"),
	n = i.val() == i[0].defaultValue ? "": i.val(),
	o = s.val() == s[0].defaultValue ? "": s.val(),
	a = this.checkCtn(n, o, t);
	// console.log(e)
	a && (this.showModal({
		type: "loading",
		title: "提交中"
	}), this.ajaxData({
		type: this.type,
		loupan: this.loupan,
		loupanid: this.loupanid,
		catid: this.catid,
		typeid: this.typeid,
		isPc: this.isPc,
		name: o,
		phone: n
	}).then(function() {
		e.showModal({
			type: "success",
			title: "提交成功",
			des: "后期的信息将会发送到您的手机上"
		}),
		setTimeout(function() {
			i.val(i[0].defaultValue),
			s.val(s[0].defaultValue),
			e.close();
			var t = $(e.nowItem).siblings("#voice");
			t = parseInt(t.length) > 0 ? t: $(e.nowItem).siblings("#video-conf"),
			parseInt(t.length) > 0 && t.attr("data-over", "show")[0].play()
		},
		500)
	},
	function(t) {
		e.showModal({
			type: "success",
			title: "提交成功",
			des: "后期的信息将会发送到您的手机上"
		}),
		setTimeout(function() {
			i.val(i[0].defaultValue),
			s.val(s[0].defaultValue),
			e.close();
			var t = $(e.nowItem).siblings("#voice");
			t = parseInt(t.length) > 0 ? t: $(e.nowItem).siblings("#head_video_box"),
			parseInt(t.length) > 0 && t.attr("data-over", "show")[0].play()
		},
		500)
	})["catch"](function() {
		e.showModal({
			type: "warning",
			title: "网络异常",
			des: "请稍后再试",
			time: 1e3
		}),
		i.blur()
	}))
},
sendData.prototype.init = function() {
	var t = this;
	$(this.ctn).on("focus",
	function() {
		var e = $(this).val();
		e == $(this)[0].defaultValue && (t.beforeColor = $(this).css("color"), $(this).css("color", t.afterColor).val(""))
	}),
	$(this.ctn).on("blur",
	function() {
		var e = $(this).val();
		"" == e && $(this).css("color", t.beforeColor).val($(this)[0].defaultValue)
	}),
	$(this.ctn).on("keydown",
	function() {
		var e = document.all ? window.event: arguments[0] ? arguments[0] : event,
		i = e.charCode || e.keyCode;
		if (13 == i) {
			var s = $(this).siblings(t.btn);
			t.after(s)
		}
	}),
	$(this.btn).on("click",
	function() {
		t.after($(this))
	})
},
sendData.prototype.checkCtn = function(t, e, i) {
	var s = t;
	return e.check && "" == e.name ? (this.showModal.showModal({
		type: "error",
		title: "格式错误",
		des: "请填写您的称呼",
		time: 1e3
	}), i.siblings(this.nameCtn).focus(), !1) : "" == s ? (this.showModal.showModal({
		type: "error",
		title: "号码为空",
		des: "请填写您的手机号码",
		time: 1e3
	}), i.siblings(this.telCtn).focus(), !1) : this.reg.test(s) ? !0 : (this.showModal.showModal({
		type: "error",
		title: "格式错误",
		des: "请正确的填写您的手机号码",
		time: 1e3
	}), i.siblings(this.telCtn).focus(), !1)
},
sendData.prototype.after = function(t) {
	var e = this,
	i = {
		check: !1,
		name: ""
	},
	s = t.siblings(this.telCtn).val(),
	n = t.siblings(this.telCtn)[0].defaultValue,
	o = t.data("name");
	if ("show" == o) {
		var a = t.siblings(this.nameCtn).val(),
		r = t.siblings(this.nameCtn)[0].defaultValue;
		a == r && (a = ""),
		i.check = !0,
		i.name = a
	}
	s == n && (s = "");
	var l = this.checkCtn(s, i, t);
	if (l) {
		var h = t.attr("data-type"),
		c = t.attr("data-typeid"),
		p = t.attr("data-pc");
		e.showModal.showModal({
			type: "loading",
			title: "提交中"
		}),
		this.showModal.ajaxData({
			loupan: this.loupan,
			loupanid: this.loupanid,
			catid: this.catid,
			phone: s,
			type: h,
			typeid: c,
			isPc: p
		}).then(function() {
			e.showModal.showModal({
				type: "success",
				title: "提交成功",
				des: "后期的信息将会发送到您的手机上"
			}),
			t.siblings(e.telCtn).val("").blur(),
			t.siblings(e.nameCtn).val("").blur()
		},
		function(t) {
			e.showModal.showModal(1 == t ? {
				type: "warning",
				title: "提交太频繁",
				des: "您提交的次数太频繁，请稍后再试",
				time: 1e3
			}: {
				type: "warning",
				title: "网络异常",
				des: "后期的信息将会发送到您的手机上",
				time: 1e3
			})
		})["catch"](function() {
			e.showModal.showModal({
				type: "warning",
				title: "网络异常",
				des: "后期的信息将会发送到您的手机上",
				time: 1e3
			})
		})
	}
},
topSlide.prototype = {
	init: function() {
		var t = this;
		return $(".column-slide-thumb li").on("click",
		function() {
			var e = $(this),
			i = e.index();
			e.addClass("slide-active").siblings().removeClass("slide-active"),
			$(".column-slide li").eq(i).siblings().fadeOut(),
			$(".column-slide li").eq(i).fadeIn(),
			t.index = i
		}),
		this.imgLength = $(".column-slide li").length,
		$("#video-conf").length > 0 || !this.loop || this.imgLength <= 1 ? !1 : (this.auto(), $(".column-slide-thumb li,.column-slide li").on("mouseover",
		function() {
			t.stop()
		}), void $(".column-slide-thumb li,.column-slide li").on("mouseout",
		function() {
			t.flag && clearTimeout(t.flag),
			t.flag = setTimeout(function() {
				t.auto()
			},
			1e3)
		}))
	},
	auto: function() {
		var t = this;
		this.inv_id = setInterval(function() {
			t.showEl.eq(t.index).siblings().fadeOut(),
			t.showEl.eq(t.index).fadeIn(),
			t.controlEl.eq(t.index).addClass("slide-active").siblings().removeClass("slide-active"),
			t.index == t.imgLength - 1 ? t.index = 0 : t.index++
		},
		this.pause)
	},
	stop: function() {
		return this.inv_id ? void clearInterval(this.inv_id) : !1
	}
},
loudong.prototype.init = function() {
	var t = this.drag.length;
	return 1 > t ? !1 : void(this.imgCheck && this.imgComplete())
},
loudong.prototype.setStyle = function(t) {
	var e = this,
	i = $(t).css("width").replace("px", ""),
	s = $(t).css("height").replace("px", ""),
	n = e.parent.css("width").replace("px", ""),
	o = e.parent.css("height").replace("px", "");
	this.center.left = .5 * parseInt(n),
	this.center.top = .5 * parseInt(o);
	var a = parseInt(i) - parseInt(n) <= 0 ? 0 : parseInt(i) - parseInt(n),
	r = parseInt(s) - parseInt(o) <= 0 ? 0 : parseInt(s) - parseInt(o),
	l = 0 == a || 2 > a ? parseInt(i) : a + parseInt(i),
	h = 0 == r || 2 > r ? parseInt(s) : r + parseInt(s);
	n > l && this.parent.find(".tips-list").css("right", (n - l) / 2 + "px"),
	this.parent.find(".tips-item").addClass("active"),
	this.parent.find(".tips-list").show(),
	e.cover.css({
		width: l + "px",
		height: h + "px",
		position: "absolute",
		top: "-" + r + "px",
		left: l < parseInt(n) ? (parseInt(n) - l) / 2 : "-" + a + "px"
	}),
	e.drag.Tdrag({
		scope: "#" + e.cover.attr("id")
	}),
	e.drag.css({
		left: 0 == a || 2 > a ? 0 : a + "px",
		top: r / 2 + "px",
		visibility: "visible"
	})
},
loudong.prototype.labelClick = function() {
	var t = this;
	return $(this.label).click(function() {
		var e = $(this).index();
		$(t.infoList).hide().eq(e).show()
	}),
	!1
},
loudong.prototype.labelInit = function() {
	for (var t = $(this.label), e = 0; e < t.length; e++) {
		var i = $(t[e]),
		s = i.data("coord").split(","),
		n = i.find(".loudong-status"),
		o = n.data("status"),
		a = parseInt(s[0]) - 14;
		1 == o ? (n.text("在售"), n.addClass("zs")) : 2 == o ? n.text("待售") : 3 == o && n.text("售罄"),
		i.css({
			left: a + "px",
			top: s[1] + "px"
		})
	}
	t.show()
},
loudong.prototype.tipsChange = function() {
	var t = this;
	$(".tips-list .tips-item").on("click",
	function() {
		for (var e = $(this).index() + 1, i = $("#drag li"), s = 0; s < i.length; s++) {
			var n = $(i[s]),
			o = n.data("type");
			o == e ? t.typeSwitch[e] ? n.hide() : n.show() : null
		}
		t.typeSwitch[e] ? $(this).removeClass("active") : $(this).addClass("active"),
		t.typeSwitch[e] = !t.typeSwitch[e]
	})
},
loudong.prototype.typeChange = function() {
	$(".loudong-ctn .type-change").on("click",
	function() {
		$(this).children(".type-list").css("display");
		$(this).children(".type-list").slideToggle(300)
	}),
	$(".loudong-ctn .type-list .type-item").on("click",
	function() {
		var t = $(this).parent().siblings();
		t.text($(this).text())
	})
},
loudong.prototype.imgComplete = function() {
	var t = this,
	e = setInterval(function() {
		t.imgEl[0].complete && (t.setStyle(t.imgEl[0]), t.labelInit(), t.labelClick(), t.tipsChange(), t.typeChange(), clearInterval(e))
	},
	50)
},
Voice.prototype = {
	init: function() {
		var t = $(this.voiceEl);
		return t.length < 1 ? !1 : (this.voiceCheck(), this.bufferedChange(), void this.voiceControl())
	},
	voiceCheck: function() {
		var t = this;
		$(this.voiceEl)[0].load(),
		$(this.voiceEl).on("canplay",
		function() {
			return t.load ? (t.duration = $(this)[0].duration, t.setTime(t.timeConversion(t.duration), !0), t.load = !1, void(t.ready = !0)) : !1
		}),
		$(this.voiceEl).on("canplaythrough",
		function() {
			t.bufferComplete = !0
		}),
		$(this.voiceEl).on("timeupdate",
		function() {
			return t.dragMove ? !1 : void t.progressBarChange($(this)[0].currentTime, "time")
		}),
		$(this.voiceEl).on("playing",
		function() {
			$(".voice-play").removeClass("switch-play").addClass("switch-pause")
		}),
		$(this.voiceEl).on("pause",
		function() {
			$(".voice-play").removeClass("switch-pause").addClass("switch-play")
		})
	},
	voiceControl: function() {
		var t = this,
		e = $(t.voiceEl)[0];
		this.progressBarWidth = parseInt($(".voice-progressBar").css("width").replace("px", "")),
		this.progressBarStart = parseInt($(".voice-progressBar").offset().left),
		this.progressBarEnd = this.progressBarWidth + this.progressBarStart,
		this.volumeLength = parseInt($(".voice-volume-progressBar").css("width").replace("px", "")),
		this.getMuted(e),
		$(".voice-cover .play-control").on("click",
		function() {
			return t.ready ? void(e.paused ? e.play() : e.pause()) : !1
		}),
		$("body").on("dragstart",
		function() {
			return ! 1
		}),
		$(".voice-control-FR").on("click",
		function() {
			if (!t.ready || !t.lock) return ! 1;
			if (e.paused) return ! 1;
			var i = e.currentTime,
			s = Math.floor(i) - 5 <= 0;
			e.currentTime = s ? 0 : i - 5
		}),
		$(".voice-control-FF").on("click",
		function() {
			if (!t.ready || !t.lock) return ! 1;
			if (e.paused) return ! 1;
			var i = e.currentTime,
			s = t.duration - (Math.floor(i) + 5) <= 0;
			e.currentTime = s ? t.duration: i + 5
		}),
		$(".voice-progressBar").on("mousedown",
		function(i) {
			if (!t.ready || !t.lock) return ! 1;
			var s = i;
			if (0 != s.button) return ! 1;
			t.progressClick = !0,
			e.paused || (t.play = !0),
			e.pause();
			var n = s.offsetX,
			o = n / t.progressBarWidth;
			t.progressBarChange(o),
			t.dragMove = !0,
			$("body").on("selectstart",
			function() {
				return ! 1
			}),
			$(document).on("mousemove",
			function(i) {
				if (!t.ready || !t.dragMove) return ! 1;
				var s = i,
				n = $(".voice-shadow");
				n.length < 1 ? $(".voice-cover").append(t.shadow) : n.show();
				var o = s.clientX;
				t.progressDrag(e, o)
			})
		}),
		$(document).on("mouseup",
		function(i) {
			var s, n = $(".voice-shadow");
			if (!t.ready) return ! 1;
			var o = i;
			return 0 != o.button ? !1 : t.dragMove ? (s && clearTimeout(s), n.length > 0 && n.hide(), s = setTimeout(function() {
				e.currentTime = t.currentTime,
				t.dragMove = !1,
				t.play && (e.play(), t.play = !1)
			},
			200), $(document).unbind("mousemove"), $("body").unbind("selectstart"), !1) : void 0
		}),
		$(".voice-progressBar-button").on("mousedown",
		function(i) {
			if (!t.ready || !t.lock) return ! 1;
			var s = i;
			return 0 != s.button ? !1 : (t.progressClick = !0, e.paused || (t.play = !0), e.pause(), t.dragMove = !0, $(document).on("mousemove",
			function(i) {
				if (!t.ready || !t.dragMove) return ! 1;
				var s = i,
				n = s.clientX;
				return t.progressDrag(e, n),
				!1
			}), !1)
		})
	},
	timeConversion: function(t) {
		var e = Math.floor(t / 60),
		i = Math.floor(t % 60);
		return i = 10 > i ? "0" + String(i) : i,
		e = 10 > e ? "0" + String(e) : e,
		{
			min: e,
			s: i
		}
	},
	getMuted: function(t) {
		var e = this;
		t.muted && (t.muted = !1),
		$(".voice-volume-switch").on("click",
		function(i) {
			if (!e.ready) return ! 1;
			var s = i;
			return 0 != s.button ? !1 : (t.muted = t.muted ? !1 : !0, void(t.muted ? ($(this).removeClass("volume-switch-off").addClass("volume-switch-on"), $(".voice-volume-progressBar-now").removeClass("voice-volume-progressBar-now-freeze")) : ($(this).removeClass("volume-switch-on").addClass("volume-switch-off"), $(".voice-volume-progressBar-now").addClass("voice-volume-progressBar-now-freeze"))))
		}),
		$(".voice-volume-progressBar").on("mousedown",
		function(i) {
			if (!e.ready) return ! 1;
			var s = i;
			if (0 != s.button) return ! 1;
			var n = s.offsetX,
			o = n / e.volumeLength;
			t.volume = o,
			e.setMuted(o)
		})
	},
	setMuted: function(t) {
		t = (100 * t).toFixed(2),
		$(".voice-volume-progressBar-now").css("width", t + "%")
	},
	progressBarChange: function(t, e) {
		t = e && "time" == e ? t: t * this.duration;
		var i = $(".pop"),
		s = $(this.voiceEl).attr("data-over");
		"show" == s && s && (this.lock = !0);
		var n = 0 == t ? 0 : (t / this.duration * 100).toFixed(2);
		return t >= this.limitTime && !this.lock ? ($(this.voiceEl)[0].pause(), i.is(":hidden") && $(this.voiceEl).siblings(".up-pop").click(), !1) : (Math.floor(t) != this.nowTime && (this.setTime(this.timeConversion(t)), this.nowTime = Math.floor(t)), (n || 0 == n) && $(".voice-progressBar-current").css("width", n + "%"), void(this.currentTime = t))
	},
	progressDrag: function(t, e) {
		var i = parseInt($(".voice-progressBar").offset().left);
		this.progressBarStart = this.progressBarStart == i ? this.progressBarStart: i,
		this.progressBarEnd = this.progressBarWidth + this.progressBarStart;
		var s = (e - this.progressBarStart) / this.progressBarWidth;
		return e <= this.progressBarStart ? (this.progressBarChange(0), !1) : e >= this.progressBarEnd ? (this.progressBarChange(1), !1) : void this.progressBarChange(s)
	},
	bufferedChange: function() {
		var t = this,
		e = $(".voice-progressBar-load"),
		i = setInterval(function() {
			if (t.bufferComplete) return clearInterval(i),
			e.animate({
				width: "100%"
			},
			1e3),
			!1;
			var s = $(t.voiceEl)[0].buffered.length;
			e.animate({
				width: 100 * s + "%"
			},
			100)
		},
		1e3)
	},
	setTime: function(t, e) {
		var i = e ? ".voice-time-all": ".voice-time-now";
		$(i + " .time-m").text(t.min),
		$(i + " .time-s").text(t.s)
	}
},
videoGET.prototype.init = function() {
	var t = !0,
	e = this;
	try {
		{
			$(this.el)[0].canPlayType
		}
	} catch(i) {
		t = !1
	}
	if (!t) return ! 1;
	if (0 == $(this.el).length) return ! 1;
	$(this.el)[0].paused;
	return 0 == $(this.el).length ? !1 : ($(this.el)[0].load(), this.loadReady(), $(this.el).on("timeupdate",
	function() {
		e.progressChange($(this)[0].currentTime)
	}), $(this.erweima).children(".close").on("click",
	function() {
		e.erweimaHide()
	}), $(this.el).on("ended",
	function() {
		e.erweimaShow()
	}), $(this.playel).on("click",
	function() {
		e.playChange($(this))
	}), void $(this.playel).hover(function() {
		$(e.conf).fadeIn()
	},
	function() {
		var t = $(e.el)[0].paused;
		t || $(e.conf).fadeOut(1500)
	}))
},
videoGET.prototype.playChange = function() {
	var t = $(this.el),
	e = $(this.el)[0].paused;
	e ? ($(this.conf).removeClass(this.playIcon).addClass(this.pauseIcon), t[0].play()) : ($(this.conf).removeClass(this.pauseIcon).addClass(this.playIcon), t[0].pause())
},
videoGET.prototype.progressChange = function(t) {
	if (t = parseFloat(t), 0 == t) return ! 1;
	var e = this.time - t <= 0 ? !0 : !1;
	return this.playing ? ($(this.el).attr("controls", "controls"), $(this.playel).hide()) : this.playing = "show" == $(this.el).data("over") ? !0 : !1,
	e && !this.playing ? ($(this.el).siblings(".up-pop").click(), $(this.conf).removeClass(this.pauseIcon).addClass(this.playIcon).fadeIn(), $(this.el)[0].pause(), !1) : void 0
},
videoGET.prototype.loadReady = function() {
	{
		var t = this;
		$(this.el)[0]
	}
	$(this.el).on("canplay",
	function() {
		var e = parseInt($(this)[0].duration);
		t.duration = e,
		t.num = parseFloat(e / 100)
	})
},
videoGET.prototype.erweimaShow = function() {
	var t = $(this.erweima);
	return ! t || t.length <= 0 ? !1 : ($(this.el)[0].controls = !1, void $(this.erweima).fadeIn())
},
videoGET.prototype.erweimaHide = function() {
	var t = this;
	$(this.erweima).fadeOut(),
	setTimeout(function() {
		$(t.el).attr("controls", "controls")
	},
	500)
};